export const VALIDATION = {
    MIN_PHONE_LENGTH: 7,
    MAX_NAME_LENGTH: 100,
    MAX_PHONE_LENGTH: 20,
    MAX_EMAIL_LENGTH: 255,
    MAX_NOTES_LENGTH: 500,
    EMAIL_REGEX: /^[^\s@]+@[^\s@]+\.[^\s@]+$/,
} as const;
